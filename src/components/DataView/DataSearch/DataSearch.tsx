import React from 'react'
import {TFilterFunc} from "../DataViewTable/DataViewTable";
//import styles from './DataSearch.module.css'

type TProps = {
    onFilterChange: (callback:TFilterFunc)=>void
}
export const DataSearch:React.FC<TProps> = (props) => {
    return (
        <></>
    )
}